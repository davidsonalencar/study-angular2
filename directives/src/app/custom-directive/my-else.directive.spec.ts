import {MyElseDirective} from './my-else.directive';

describe('MyElseDirective', () => {
    it('should create an instance', () => {
        const directive = new MyElseDirective();
        expect(directive).toBeTruthy();
    });
});
